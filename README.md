# CrashCatcher

CrashCatcher is a RimWorld debug mod that intercepts the first world-tick failure after load, writes a dedicated crash report, and holds the game paused instead of letting the crash continue.
